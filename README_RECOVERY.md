# Recovered Android project

This folder was generated from `base.apk`.

IMPORTANT: an APK is a compiled artifact, not the original Android Studio project.
The original source/Gradle structure cannot be guaranteed to be recovered exactly.

APK size: 14869984 bytes
DEX files found: 16
Native .so files found: 4

The complete APK extraction is preserved under `apk_contents/`.
Use this as the recovery workspace. The next step is decompilation/reconstruction
of the application's own DEX code and manifest/resources into `app/src/main/`.
